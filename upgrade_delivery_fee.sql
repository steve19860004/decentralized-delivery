-- =========================================
-- 升級：浮動運費與 0 抽成模式資料庫更新
-- =========================================

-- 在任務池新增獨立的運費欄位，以便精準備紀錄外送員的 100% 跑腿收入
-- 舊有的 total_amount 依然代表「客人總付金額 (餐點 + 運費)」
ALTER TABLE jobs ADD COLUMN IF NOT EXISTS delivery_fee INTEGER DEFAULT 0;
